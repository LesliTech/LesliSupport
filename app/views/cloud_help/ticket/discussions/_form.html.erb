<%= form_with(model: ticket_discussion, local: true) do |form| %>
  <% if ticket_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_discussion.errors.count, "error") %> prohibited this ticket_discussion from being saved:</h2>

      <ul>
        <% ticket_discussion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
