<%= form_with(model: ticket_follow, local: true) do |form| %>
  <% if ticket_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_follow.errors.count, "error") %> prohibited this ticket_follow from being saved:</h2>

      <ul>
        <% ticket_follow.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
