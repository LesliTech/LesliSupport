<%= form_with(model: ticket_activity, local: true) do |form| %>
  <% if ticket_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_activity.errors.count, "error") %> prohibited this ticket_activity from being saved:</h2>

      <ul>
        <% ticket_activity.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
