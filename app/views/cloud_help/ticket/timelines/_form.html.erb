<%= form_with(model: ticket_timeline, local: true) do |form| %>
  <% if ticket_timeline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_timeline.errors.count, "error") %> prohibited this ticket_timeline from being saved:</h2>

      <ul>
        <% ticket_timeline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
