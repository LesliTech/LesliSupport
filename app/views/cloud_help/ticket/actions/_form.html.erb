<%= form_with(model: ticket_action, local: true) do |form| %>
  <% if ticket_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_action.errors.count, "error") %> prohibited this ticket_action from being saved:</h2>

      <ul>
        <% ticket_action.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
