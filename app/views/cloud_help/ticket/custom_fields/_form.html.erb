<%= form_with(model: ticket_custom_field, local: true) do |form| %>
  <% if ticket_custom_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_custom_field.errors.count, "error") %> prohibited this ticket_custom_field from being saved:</h2>

      <ul>
        <% ticket_custom_field.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
