<%= form_with(model: ticket_follower, local: true) do |form| %>
  <% if ticket_follower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_follower.errors.count, "error") %> prohibited this ticket_follower from being saved:</h2>

      <ul>
        <% ticket_follower.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
