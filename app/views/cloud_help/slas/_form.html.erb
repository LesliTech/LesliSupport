<%= form_with(model: sla, local: true) do |form| %>
  <% if sla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sla.errors.count, "error") %> prohibited this sla from being saved:</h2>

      <ul>
        <% sla.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
