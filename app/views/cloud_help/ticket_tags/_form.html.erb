<%= form_with(model: ticket_tag, local: true) do |form| %>
  <% if ticket_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_tag.errors.count, "error") %> prohibited this ticket_tag from being saved:</h2>

      <ul>
        <% ticket_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
