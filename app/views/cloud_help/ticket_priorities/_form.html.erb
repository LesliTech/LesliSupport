<%= form_with(model: ticket_priority, local: true) do |form| %>
  <% if ticket_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_priority.errors.count, "error") %> prohibited this ticket_priority from being saved:</h2>

      <ul>
        <% ticket_priority.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
