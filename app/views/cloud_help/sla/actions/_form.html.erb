<%= form_with(model: sla_action, local: true) do |form| %>
  <% if sla_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sla_action.errors.count, "error") %> prohibited this sla_action from being saved:</h2>

      <ul>
        <% sla_action.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
