<%= form_with(model: sla_association, local: true) do |form| %>
  <% if sla_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sla_association.errors.count, "error") %> prohibited this sla_association from being saved:</h2>

      <ul>
        <% sla_association.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
