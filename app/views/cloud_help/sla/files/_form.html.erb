<%= form_with(model: sla_file, local: true) do |form| %>
  <% if sla_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sla_file.errors.count, "error") %> prohibited this sla_file from being saved:</h2>

      <ul>
        <% sla_file.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
