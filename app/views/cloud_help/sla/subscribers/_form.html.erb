<%= form_with(model: sla_subscriber, local: true) do |form| %>
  <% if sla_subscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sla_subscriber.errors.count, "error") %> prohibited this sla_subscriber from being saved:</h2>

      <ul>
        <% sla_subscriber.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
