<%= form_with(model: workflow_state, local: true) do |form| %>
  <% if workflow_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_state.errors.count, "error") %> prohibited this workflow_state from being saved:</h2>

      <ul>
        <% workflow_state.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
