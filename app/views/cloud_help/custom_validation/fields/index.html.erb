<p id="notice"><%= notice %></p>

<h1>Custom Validation Fields</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @custom_validation_fields.each do |custom_validation_field| %>
      <tr>
        <td><%= link_to 'Show', custom_validation_field %></td>
        <td><%= link_to 'Edit', edit_custom_validation_field_path(custom_validation_field) %></td>
        <td><%= link_to 'Destroy', custom_validation_field, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Custom Validation Field', new_custom_validation_field_path %>
