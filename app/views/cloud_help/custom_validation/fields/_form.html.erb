<%= form_with(model: custom_validation_field, local: true) do |form| %>
  <% if custom_validation_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_validation_field.errors.count, "error") %> prohibited this custom_validation_field from being saved:</h2>

      <ul>
        <% custom_validation_field.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
