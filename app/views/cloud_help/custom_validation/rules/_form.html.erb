<%= form_with(model: custom_validation_rule, local: true) do |form| %>
  <% if custom_validation_rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_validation_rule.errors.count, "error") %> prohibited this custom_validation_rule from being saved:</h2>

      <ul>
        <% custom_validation_rule.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
