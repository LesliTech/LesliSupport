<%= form_with(model: custom_validation, local: true) do |form| %>
  <% if custom_validation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_validation.errors.count, "error") %> prohibited this custom_validation from being saved:</h2>

      <ul>
        <% custom_validation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
