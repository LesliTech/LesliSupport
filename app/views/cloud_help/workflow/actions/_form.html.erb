<%= form_with(model: workflow_action, local: true) do |form| %>
  <% if workflow_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_action.errors.count, "error") %> prohibited this workflow_action from being saved:</h2>

      <ul>
        <% workflow_action.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
