<%= form_with(model: workflow_association, local: true) do |form| %>
  <% if workflow_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_association.errors.count, "error") %> prohibited this workflow_association from being saved:</h2>

      <ul>
        <% workflow_association.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
