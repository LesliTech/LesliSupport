<%= form_with(model: workflow_detail, local: true) do |form| %>
  <% if workflow_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_detail.errors.count, "error") %> prohibited this workflow_detail from being saved:</h2>

      <ul>
        <% workflow_detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
