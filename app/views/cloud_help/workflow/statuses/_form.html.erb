<%= form_with(model: workflow_status, local: true) do |form| %>
  <% if workflow_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_status.errors.count, "error") %> prohibited this workflow_status from being saved:</h2>

      <ul>
        <% workflow_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
