<%= form_with(model: ticket_workflow, local: true) do |form| %>
  <% if ticket_workflow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_workflow.errors.count, "error") %> prohibited this ticket_workflow from being saved:</h2>

      <ul>
        <% ticket_workflow.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
