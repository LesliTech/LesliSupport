<%= form_with(model: catalog_ticket_source, local: true) do |form| %>
  <% if catalog_ticket_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catalog_ticket_source.errors.count, "error") %> prohibited this catalog_ticket_source from being saved:</h2>

      <ul>
        <% catalog_ticket_source.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
