<%= form_with(model: catalog_ticket_priority, local: true) do |form| %>
  <% if catalog_ticket_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catalog_ticket_priority.errors.count, "error") %> prohibited this catalog_ticket_priority from being saved:</h2>

      <ul>
        <% catalog_ticket_priority.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
