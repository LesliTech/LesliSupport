<%= form_with(model: catalog_ticket_category, local: true) do |form| %>
  <% if catalog_ticket_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catalog_ticket_category.errors.count, "error") %> prohibited this catalog_ticket_category from being saved:</h2>

      <ul>
        <% catalog_ticket_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
