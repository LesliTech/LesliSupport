<%
group = Date2.new.date.db_column(:created_at)
tickets = current_user.account.support.tickets
    .group(group.sub('as created_at_string',''))
    .order(created_at: :asc)
    .limit(10)
    .count
%>

<%= render(LesliView::Widgets::Chart.new('Tickets by date', tickets, type: :line)) %>
