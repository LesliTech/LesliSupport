en:
    activerecord:
        errors:
            models:
                cloud_help/ticket_workflow:
                        attributes:
                            base:
                                foreign_key_prevents_destruction: Please verify that there are no tickets in this workflow before attempting to modify it
