en:
    cloud_help:
        ticket_states:
            form:
                messages:
                    update:
                        successful: Ticket state updated successfully
                    create:
                        successful: Ticket state created successfully
                actions:
                    update: Update state
                    create: Create state
