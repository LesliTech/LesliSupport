en:
    cloud_help:
        workflows:
            show:
                modal:
                    title: Are you sure you want to delete this workflow?
                    body: Once deleted, you will not be able to recover this workflow. Note that you can only delete a workflow if it is not associated to any ticket and it is not the default workflow.
                    actions:
                        delete: Yes, delete it
                        cancel: Cancel
                messages:
                    set_as_default: This workflow was been successfully set as default
                    delete: Workflow deleted successfully
